using System;

namespace PatternMatching.LiveryRegistration
{
    public class Taxi
    {
        public Taxi(int fares)
        {
            Fares = fares;
        }

        public int Fares { get; set; }
    }
}
